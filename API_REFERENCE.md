# Udseen 脚本引擎 API 参考

> 本文档列出所有可在 `.ykn` 脚本中使用的内置函数、工厂对象和对象方法。

---

## 目录

1. [基本语法](#1-基本语法)
2. [全局内置函数](#2-全局内置函数)
3. [工厂对象 — 创建场景元素](#3-工厂对象--创建场景元素)
4. [SceneObject 方法（角色/背景/文字）](#4-sceneobject-方法角色背景文字)
   - [生命周期](#41-生命周期)
   - [动图控制](#42-动图控制)
   - [位置与变换](#43-位置与变换)
   - [透明度与层级](#44-透明度与层级)
   - [颜色滤镜](#45-颜色滤镜)
   - [高级滤镜](#46-高级滤镜)
   - [文字属性（仅 Text 类型）](#47-文字属性仅-text-类型)
   - [对话](#48-对话)
5. [Audio 对象方法](#5-audio-对象方法)
6. [Filter 对象方法](#6-filter-对象方法)
7. [ObjectFunction — 自定义对象方法](#7-objectfunction--自定义对象方法)
8. [数据类型](#8-数据类型)

---

## 1. 基本语法

### 声明变量

```
let 变量名 = 初始值;
```
像给自己的"存储盒"贴标签一样，你给一段数据取个名字存起来。

```
let score = 100;         // 存一个数字 100，名字叫 score
let name = "Alice";      // 存一段文字"Alice"，名字叫 name
let flag;                // 定义一个空变量，默认值是 0
```

### 修改变量的值

```
变量名 = 新值;
变量名[位置] = 新值;   // 修改列表或映射中的某个位置
```

### 语句结构

| 指令 | 作用 | 例子 |
|---|---|---|
| `if` | 如果条件成立就执行，否则执行 else | `if (score > 50) { ... } else { ... }` |
| `while` | 反复执行，直到条件不成立 | `while (count < 5) { ... }` |
| `choice` | 给玩家弹出选项按钮 | `choice { case "开门" { ... } case "离开" { ... } }` |
| `async` | 组里的所有指令**同时**开始执行 | `async { bgm.play(); bg.begin(); }` |
| `wait(n)` | 先等 n 毫秒，再继续往下 | `wait(1000) { char.visible(true); }` |

### 自定义函数

```
function 名字(参数1?, 参数2?) { ... }
```

---

## 2. 全局内置函数

### `say(text, audio?)`

**作用**：在对话框里显示一行台词，等玩家点一下屏幕才继续。这是视觉小说最核心的指令。

| 参数 | 说明 |
|---|---|
| `text` | 要显示的话 |
| `audio` | （可选）说话时同时播放的语音文件路径 |

```
say("你好，旅行者。");
say("欢迎来到这个世界。", "voice/greeting.mp3");   // 带语音
```

### `pause()`

**作用**：画面停住，等玩家点一下屏幕再继续。用于让玩家停顿、消化剧情。

```
pause();
```

### `speech(visible)`

**作用**：控制对话框的显示/隐藏。旁白、回忆、内心独白等场景不需要对话框时可以用。

| 参数 | 说明 |
|---|---|
| `visible` | `0` = 藏起对话框，之后的 `say` 也不弹出来；`1` = 恢复对话框 |

```
speech(0);             // 藏起对话框
say("这是旁白，没有对话框");   // 不会显示对话框
speech(1);             // 恢复
```

### `Math.*`

数学小工具，用 `Math.函数名(...)` 调用。

| 函数 | 作用 | 例子 |
|---|---|---|
| `random()` | 生成 0~1 之间的随机小数 | `Math.random()` → 0.437 |
| `floor(x)` | 去掉小数，只留整数部分 | `Math.floor(3.99)` → 3 |
| `ceil(x)` | 进一位取整 | `Math.ceil(3.01)` → 4 |
| `round(x)` | 四舍五入 | `Math.round(3.5)` → 4 |
| `abs(x)` | 去掉负号 | `Math.abs(-5)` → 5 |
| `min(a,b,...)` | 从一堆数里挑最小的 | `Math.min(10, 3, 8)` → 3 |
| `max(a,b,...)` | 从一堆数里挑最大的 | `Math.max(10, 3, 8)` → 10 |
| `PI` | 圆周率 3.14159... | — |

```
let r = Math.random();          // r 可能是 0.123, 0.874...
let d = Math.floor(3.14);       // d = 3
```

### `parallel(...animations)`

**作用**：让好几个动作**同时进行**，全部完成了才继续往下。比如背景变暗的同时角色入场动画也在播放。

| 参数 | 说明 |
|---|---|
| `animations` | 几个要同时做的动作，每个用 `() => 对象.动作()` 的形式写 |

```
parallel(
  () => bg.alpha(0.5, 1000),       // 背景半秒变半透明
  () => char.moveToX(100, 1000)    // 同时角色向右移动
);
// 两个都完成了才继续
```

### `sequence(...animations)`

**作用**：让多个动作**一个接一个**执行，前一个做完了才做下一个。

---

## 3. 工厂对象 — 创建场景元素

### `Background.set(图片路径)` / `Character.set(图片路径, 显示名字?, 头像路径?)`

**作用**：加载一张图片（背景或角色），之后可以用它控制位置、大小、滤镜等所有属性。

| 参数 | 必填 | 说明 |
|---|---|---|
| `图片路径` | 是 | 图片文件在项目中的位置（.png / .jpg / .gif） |
| `显示名字` | 否 | 对话框里显示的名字（比如角色文件叫 alice.png，这里可以设成"爱丽丝"） |
| `头像路径` | 否 | 对话框里小头像的图片路径（仅角色可用） |

```
let bg = Background.set("forest_day.png");
let char = Character.set("alice.png", "爱丽丝", "alice_face.png");
```

### `Text.set(内容)`

**作用**：创建直接显示在画面上的文字。不是对话框台词，而是标题、字幕、提示文字等。

| 参数 | 说明 |
|---|---|
| `内容` | 要显示的文本 |

```
let title = Text.set("第一章：启程");   // 画面上出现"第一章：启程"这几个字
```

### `Audio.set(音频路径)`

**作用**：加载一个音频文件，之后可以用它控制播放、暂停、音量。

| 参数 | 说明 |
|---|---|
| `音频路径` | 音频文件路径（.mp3 / .ogg / .wav） |

```
let bgm = Audio.set("music/peaceful.mp3");
```

### `Filter.set()`

**作用**：创建一个"屏幕滤镜"对象，可以给整个画面叠加颜色、模糊、扭曲等效果。不需要参数。

```
let fade = Filter.set();
```

---

## 4. SceneObject 方法（角色/背景/文字）

> 所有方法用 `对象.方法名(参数)` 的形式调用。

### 4.1 生命周期

#### `begin(visible?)`

**作用**：把之前创建的对象放到画面上显示出来。不调用 `begin()`，对象就只是一个"数据"，画面里看不到。

| 参数 | 必填 | 说明 |
|---|---|---|
| `visible` | 否 | 是否立即显示。`true` 或 `1` = 显示在画面上；`false` 或 `0` = 加入但先藏起来 |

```
bg.begin();                  // 背景显示出来
textObj.begin(false);        // 文字加入舞台但先隐藏
bg.begin(1);                 // 1 也代表显示
```

> 如果图片是 GIF 动图，`begin()` 会自动开始播放。

---

#### `autobegin(visible?)`

**作用**：让对象闪一下（显示 50 毫秒）就自动消失。适合火花、闪光提示等一闪而过的特效。

| 参数 | 必填 | 说明 |
|---|---|---|
| `visible` | 否 | 是否显示 |

```
spark.autobegin();      // 闪一下就没了
```

---

#### `end()`

**作用**：彻底删除对象——从画面上移除，同时释放占用的所有资源（图片、滤镜、动图内存）。

```
bg.end();               // 背景消失
```

---

#### `visible(able, time?)`

**作用**：让对象显示或隐藏。可以带渐变过渡——慢慢消失（渐隐）或慢慢出现（渐显）。

| 参数 | 必填 | 说明 |
|---|---|---|
| `able` | 是 | `true` = 显示出来；`false` = 隐藏 |
| `time` | 否 | 渐变过程耗时（毫秒）。例：`500` = 半秒内缓缓淡出/淡入；`0` = 瞬间切换，无过渡动画 |

```
char.visible(false, 500);    // 角色在半秒内渐渐淡出消失
char.visible(true, 300);     // 角色在 0.3 秒内渐渐出现
char.visible(false);         // 角色瞬间消失
```

---

### 4.2 动图控制

> 以下方法**仅限 GIF 动图**。静态图片调用了不会报错，但也没效果。

#### `loop()`

**作用**：让 GIF 动图循环播放。

```
sprite.loop();   // 动图开始循环播放
```

#### `pauseAnim()`

**作用**：让正在播放的动图定格在当前帧——画面不动了。

```
sprite.pauseAnim();   // 动图暂停
```

#### `stopAnim()`

**作用**：停止播放并回到第一帧。

```
sprite.stopAnim();   // 动图回到开头
```

#### `setAnimSpeed(multiplier)`

**作用**：控制动图播放是快放还是慢放。

| 参数 | 必填 | 说明 |
|---|---|---|
| `multiplier` | 是 | 速度倍数。`2` = 2 倍速快放；`0.5` = 慢动作（半速播放）；最小 `0.1` |

```
sprite.setAnimSpeed(2);     // 2 倍速，动作加快
sprite.setAnimSpeed(0.5);   // 慢动作效果
```

#### `setFPS(fps)`

**作用**：手动设置动图每秒播放多少帧。帧数越高动画越流畅顺滑，但性能消耗也越大。

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| `fps` | 是 | `16` | 每秒播放的帧数。越高越丝滑，建议 24 够用 |

```
sprite.setFPS(24);    // 设成 24 帧/秒，更流畅
```

#### `showFrame(index)`

**作用**：直接跳到动图的某一帧画面。序号从 1 开始（第 1 帧 = 第一张画面）。

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| `index` | 是 | `1` | 要跳到的帧序号（1, 2, 3...） |

```
sprite.showFrame(3);   // 跳到动图的第 3 帧
```

---

### 4.3 位置与变换

> **画面坐标**：`(0, 0)` = 屏幕正中心。
> - **左右**：正数向右，负数向左
> - **上下**：正数向上，负数向下
>
> 比如 `(200, 100)` = 屏幕中心偏右 200 像素、偏上 100 像素的位置。

#### `move(dx, dy, time?)`

**作用**：让对象从当前位置**往某个方向走一段距离**（不改变原本的位置定义）。比如"向右走 100 像素"。

| 参数 | 必填 | 说明 |
|---|---|---|
| `dx` | 是 | 左右移动多少像素。正数=向右，负数=向左。`100` = 向右走 100 步 |
| `dy` | 是 | 上下移动多少像素。正数=向上，负数=向下。`-50` = 向下走 50 步 |
| `time` | 否 | 走过去耗时多久（毫秒）。`500` = 半秒走到；`0` = 瞬移 |

```
char.move(100, 0, 500);        // 角色向右移动 100 像素，耗时 0.5 秒
char.move(0, -50);             // 角色向下移动 50 像素
char.move(200, 200, 1000);     // 向右上方移动，耗时 1 秒
```

> 直观理解：告诉角色"往右走 100 步，用 0.5 秒走完"。

---

#### `setPos(x, y, time?)`

**作用**：让对象**直接去到**画面上的某个坐标位置——不管它之前在哪儿，直接去指定位置。

| 参数 | 必填 | 说明 |
|---|---|---|
| `x` | 是 | 目标位置的 X 坐标。也可以用 `{posX: 200, posY: 100}` 这种写法一次传两个 |
| `y` | 否 | 目标位置的 Y 坐标（不传就保持当前 Y） |
| `time` | 否 | 走过去耗时（毫秒） |

```
char.setPos(0, 0, 1000);               // 1 秒后角色走到屏幕正中央
char.setPos(200, 100, 500);            // 0.5 秒走到横向 200、纵向 100 的位置
char.setPos({posX: 200, posY: 100}, 500);  // 和上面等价
```

> 对比 `move`：`move` 是"向右走 50 步"，`setPos` 是"走到 x=200 的位置"。

---

#### `moveToX(x, time?)`

**作用**：只改变左右位置，上下不动。

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| `x` | 是 | — | 目标 X 坐标（正=偏右，负=偏左） |
| `time` | 否 | 1000 | 走过去耗时（毫秒） |

```
char.moveToX(200, 800);   // 角色花 0.8 秒水平移动到右方 200 的位置
```

#### `moveToY(y, time?)`

**作用**：只改变上下位置，左右不动。

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| `y` | 是 | — | 目标 Y 坐标（正=偏上，负=偏下） |
| `time` | 否 | 1000 | 走过去耗时（毫秒） |

```
char.moveToY(100, 800);   // 角色花 0.8 秒垂直移动到上方 100 的位置
```

---

#### `rotate(angle, time?)`

**作用**：让对象旋转到某个**固定的朝向**。`0` = 正立；`90` = 顺时针转 90 度（歪头）；`180` = 倒立翻转。

| 参数 | 必填 | 说明 |
|---|---|---|
| `angle` | 是 | 目标角度（度）。`0`=正立，`90`=右转 90°，`180`=倒立，`-90`=左转 90° |
| `time` | 否 | 旋转过程耗时（毫秒） |

```
wheel.rotate(180, 2000);   // 轮子花 2 秒旋转到倒立状态
wheel.rotate(0, 500);      // 0.5 秒恢复正立
```

---

#### `rotateTo(angle, time?)`

**作用**：让对象**在当前角度基础上再转 X 度**。比如先转了 90 度，再 `rotateTo(90)` 就变成 180 度——相当于每次加 90 度。

| 参数 | 必填 | 说明 |
|---|---|---|
| `angle` | 是 | 在现有角度上**增加**多少度。正数=顺时针，负数=逆时针 |
| `time` | 否 | 旋转过程耗时（毫秒） |

```
wheel.rotateTo(90, 500);    // 从当前角度再顺时针转 90 度
wheel.rotateTo(90, 500);    // 再转 90 度（现在总共 180 度了）
```

> `rotate(180)` = "你给我转到 180 度的位置"；`rotateTo(90)` = "你再从这个位置转 90 度"。

---

#### `scale(val, time?)`

**作用**：把对象放大或缩小到指定倍率。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 目标大小倍率。`1`=原始大小；`2`=放大一倍（变大一倍）；`0.5`=缩小到一半 |
| `time` | 否 | 缩放动画耗时（毫秒） |

```
char.scale(1.5, 300);    // 角色在 0.3 秒内放大到 1.5 倍
char.scale(1, 500);      // 0.5 秒恢复原始大小
char.scale(0.2, 1000);   // 1 秒内缩小到 20%
```

---

#### `scaleTo(multiplier, time?)`

**作用**：在**当前大小**基础上再放大或缩小。比如已经放大了 2 倍，再 `scaleTo(2)` 就变成 4 倍。

| 参数 | 必填 | 说明 |
|---|---|---|
| `multiplier` | 是 | 在当前基础上再乘的倍率。`2` = 再大一倍；`0.5` = 缩到当前的一半 |
| `time` | 否 | 动画耗时（毫秒） |

```
char.scaleTo(2, 500);    // 当前基础上再放大一倍
char.scaleTo(0.5, 500);  // 缩到当前的一半
```

---

#### `scaleX(val, time?)` / `scaleY(val, time?)`

**作用**：只控制宽度（X 轴）或只控制高度（Y 轴）的缩放。可以把图片拉宽（变胖）或拉长（变高）。

| 参数 | 说明 |
|---|---|
| `val` | 该方向缩放倍率。`1`=原始；`2`=该方向拉大一倍 |

```
char.scaleX(2, 500);     // 角色横向拉宽到 2 倍（变胖了）
char.scaleY(1.5, 300);   // 角色纵向拉长到 1.5 倍（变高了）
```

#### `scaleXTo(multiplier, time?)` / `scaleYTo(multiplier, time?)`

**作用**：在当前宽/高的基础上再放大或缩小。

#### `setScale(sx, sy?, time?)`

**作用**：同时设置宽度和高度的缩放倍率，可以不同（用来拉伸变形）。

| 参数 | 必填 | 说明 |
|---|---|---|
| `sx` | 是 | 宽度缩放。只传一个值则宽高等比例缩放 |
| `sy` | 否 | 高度缩放（不传则和 sx 一样） |
| `time` | 否 | 动画耗时（毫秒） |

```
char.setScale(1.5);             // 宽高一起放大到 1.5 倍
char.setScale(2, 1, 500);       // 宽度 2 倍、高度不变→角色被横向拉宽，耗时 0.5 秒
```

---

### 4.4 透明度与层级

#### `alpha(val, time?)`

**作用**：控制对象的透明程度。`1` = 完全不透明（正常看到）；`0` = 完全透明（看不见）；`0.3` = 像磨砂玻璃一样隐约可见后面的东西。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 不透明度。范围 `0`~`1`。`1`=不透明看得见；`0`=完全透明看不见；`0.5`=半透明 |
| `time` | 否 | 渐变过程耗时（毫秒）。例：`1000` 就是 1 秒内慢慢变透明/变不透明 |

```
char.alpha(0.3, 1000);    // 角色在 1 秒内渐渐变到 30% 透明度，能隐约看到背后的背景
char.alpha(1, 500);       // 0.5 秒内渐渐变回完全不透明
char.alpha(0.8);          // 瞬间变到 80%（无过渡动画）
```

---

#### `index(z)`

**作用**：控制前后遮挡关系——数字越大，显示得越靠近观众（会盖住数字小的对象）。

| 参数 | 必填 | 说明 |
|---|---|---|
| `z` | 是 | 层级值。数字越大越靠前（越靠近观众，挡住后面的） |

```
bg.index(0);               // 背景放最底层
char.index(1);             // 角色在背景之上（盖住背景）
text.index(2);             // 文字在最前面（不会被挡住）
```

> 就像叠纸牌：index 越大，越在最上面。

---

### 4.5 颜色滤镜

> **三个参数的通用含义**（所有滤镜都一样）：
> 1. **`val`**：这个滤镜的核心控制值。填 `0` 通常表示"没有效果"（恢复原样）
> 2. **`time`**：过渡过程要持续多久。比如 `500` = 半秒内渐变过去，`0` = 瞬间变化
> 3. **`intensity`**：这个滤镜的效果"掺入"多少到画面中。`1` = 完全生效；`0.5` = 效果只有一半强度（半透明地叠加）；`0` = 无效

#### `blur(val, time?, intensity?)`

**作用**：让画面变模糊——就像近视没戴眼镜、或者相机没对准焦。适合梦境、回忆、失神、醒来等效果。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 模糊程度。`0`=完全清晰；越大越糊。试试 `5`（微虚化）、`15`（很糊）、`30`（几乎看不清） |
| `time` | 否 | 渐变到模糊（或恢复清晰）的过渡耗时（毫秒） |
| `intensity` | 否 | 效果混合强度。`1`=完全模糊；`0.5`=只糊一半 |

```
char.blur(10, 500);       // 角色在 0.5 秒内慢慢变模糊（模糊度 10）
char.blur(0, 300);        // 0.3 秒恢复清晰
char.blur(20);            // 瞬间变得非常模糊
```

> 视觉类比：`blur(5)` ≈ 隔着磨砂玻璃看；`blur(20)` ≈ 溺水时眼前一片模糊。

---

#### `brightness(val, time?, intensity?)`

**作用**：调整整张图的亮度。调暗做黑夜/阴天效果；调亮做闪光/曝光/梦境效果。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 亮度倍率。`1`=原始亮度（正常）；`0`=全黑（什么都看不到）；`2`=非常亮（过曝） |
| `time` | 否 | 渐变动画耗时（毫秒） |
| `intensity` | 否 | 效果混合强度 |

```
bg.brightness(0.5, 1000);    // 1 秒内变暗到一半亮度 → 天黑氛围
bg.brightness(0.3, 800);     // 更暗，接近黑夜
bg.brightness(1, 500);       // 恢复原始亮度
bg.brightness(1.5, 200);     // 快速闪亮（闪光弹效果）
```

> 视觉类比：`brightness(1)` = 正常；`brightness(0.5)` = 像戴了墨镜；`brightness(0)` = 关灯全黑。

---

#### `contrast(val, time?, intensity?)`

**作用**：控制明暗对比。对比度高 → 黑更黑、白更白，画面锐利硬朗；对比度低 → 画面灰蒙蒙的，像老照片或雾中场景。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 对比度倍率。`1`=正常；大于 1（如 `1.5`）→ 黑白更分明、更锐利；小于 1（如 `0.5`）→ 画面发灰、柔和 |
| `time` | 否 | 渐变动画耗时（毫秒） |
| `intensity` | 否 | 效果混合强度 |

```
bg.contrast(1.5, 500);       // 0.5 秒内增强对比度→画面更锐利清晰
bg.contrast(0.5, 1000);      // 1 秒内降低对比度→画面变灰，像老电影回忆
bg.contrast(1, 300);         // 恢复正常
```

---

#### `saturation(val, time?, intensity?)`

**作用**：控制颜色鲜艳程度。`0` = 变成黑白（失去所有颜色）；`1` = 正常颜色；大于 1 = 颜色非常浓艳。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 饱和度倍率。`1`=正常色彩；`0`=完全去色变黑白；`2`=颜色非常浓烈艳丽 |
| `time` | 否 | 渐变动画耗时（毫秒） |
| `intensity` | 否 | 效果混合强度 |

```
bg.saturation(0, 1000);      // 1 秒内渐渐褪色变成黑白（回忆杀、死亡场景）
bg.saturation(0.5, 500);     // 颜色变得淡淡的、柔和
bg.saturation(2, 500);       // 颜色变得非常艳丽浓烈
bg.saturation(1, 300);       // 恢复正常
```

---

#### `gamma(val, time?, intensity?)`

**作用**：调整画面中间亮度区域的"通透感"。比 `brightness` 更细腻——不影响最亮和最暗的部分，主要改变画面的"灰阶"感觉。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 伽马值。`1`=正常；小于 1（如 `0.8`）→ 整体变亮、画面更通透；大于 1（如 `1.2`）→ 整体变暗、更压抑 |
| `time` | 否 | 渐变动画耗时（毫秒） |
| `intensity` | 否 | 效果混合强度 |

```
bg.gamma(0.8, 500);     // 画面变得亮一些、更通透（适合明朗场景）
bg.gamma(1.3, 500);     // 画面变得暗沉（适合阴郁场景）
```

---

#### `rgb(r, g, b, time?, intensity?)`

**作用**：在画面上叠一层颜色——就像在镜头前放了张有色玻璃纸。可以营造氛围：红色调=夕阳/火焰；蓝色调=水下/月光；绿色调=中毒/阴森。

| 参数 | 必填 | 说明 |
|---|---|---|
| `r` | 是 | 红色的量（0~255）。`255`=加最多红色；`0`=不加红色 |
| `g` | 是 | 绿色的量（0~255） |
| `b` | 是 | 蓝色的量（0~255） |
| `time` | 否 | 颜色渐变的过渡耗时（毫秒） |
| `intensity` | 否 | 颜色叠加的总体强度 |

```
bg.rgb(255, 100, 100, 500);   // 0.5 秒叠加红色调→画面变暖（像夕阳/火光氛围）
bg.rgb(100, 100, 255, 500);   // 蓝色调→冷色氛围（水下/月夜）
bg.rgb(0, 255, 0, 500);       // 绿色调→诡异/中毒效果
```

> 视觉理解：三个数值越高，那种颜色就越浓。`(255, 255, 255)` = 全白；`(0, 0, 0)` = 全黑。

---

#### `hex(color, time?, intensity?)`

**作用**：和 `rgb` 完全一样的效果，但颜色用网页十六进制码写——如果你会用取色工具（Photoshop等），这个更方便。

| 参数 | 必填 | 说明 |
|---|---|---|
| `color` | 是 | 颜色代码，以 `#` 开头。如 `"#FF6600"` = 橙色，`"#66CCFF"` = 天蓝 |
| `time` | 否 | 过渡耗时（毫秒） |
| `intensity` | 否 | 颜色叠加强度 |

```
bg.hex("#FF6600", 500);    // 叠加橙色（黄昏氛围）
bg.hex("#66CCFF", 500);    // 叠加浅蓝色（清爽氛围）
```

> 常用颜色速查：`#FF0000`=红，`#00FF00`=绿，`#0000FF`=蓝，`#FFFF00`=黄，`#FFFFFF`=白，`#000000`=黑

---

#### `setTint(color)`

**作用**：给对象设置一个固定色调（没有渐变过渡，瞬间生效）。用数字形式的十六进制。

| 参数 | 必填 | 说明 |
|---|---|---|
| `color` | 是 | 颜色值，如 `0xFF6600`（注意：这里不是字符串，没有引号） |

```
bg.setTint(0xFF6600);       // 瞬间加上橙色色调
```

---

#### `clearFilters()`

**作用**：清除之前设置的所有颜色滤镜，让对象恢复原本的颜色外观。

```
char.clearFilters();    // 角色恢复原样
```

---

### 4.6 高级滤镜

#### `glow(val, time?, intensity?)`

**作用**：让对象边缘发出柔和的光晕——像赛博朋克里的霓虹灯效果，或者角色的"圣光"感。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 发光强度。`0`=不发光的正常状态；越大发光越强越扩散。试试 `5~20` |
| `time` | 否 | 发光渐变的过渡耗时（毫秒） |
| `intensity` | 否 | 效果混合强度 |

```
char.glow(15, 1000);   // 角色 1 秒内渐渐发出光晕
```

---

#### `dropShadow(val, time?, intensity?)`

**作用**：在对象背后生成投影，产生立体感和深度感——就像角色站在灯光下，背后有影子。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 投影强度。`0`=无投影；越大投影越明显 |
| `time` | 否 | 过渡耗时（毫秒） |
| `intensity` | 否 | 效果混合强度 |

---

#### `noise(val, time?, intensity?)`

**作用**：在画面上叠加一层"雪花噪点"——像老旧电视机的雪花屏效果。适合恐怖/录像带/复古风格。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 噪点强度。`0`=无噪点；越大噪点越多越密 |
| `time` | 否 | 渐变过渡耗时（毫秒） |
| `intensity` | 否 | 效果混合强度 |

---

#### `bw(val, time?, intensity?)`

**作用**：让画面褪去颜色变成黑白（或任意程度的去色）。`0`=彩色；`1`=完全黑白。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 黑白化程度。`0`=保持彩色；`1`=完全黑白；`0.5`=半彩色半黑白（颜色很淡） |
| `time` | 否 | 过渡耗时（毫秒） |
| `intensity` | 否 | 效果混合强度 |

```
bg.bw(1, 1000);   // 1 秒内从彩色渐变成黑白
bg.bw(0.5, 500);  // 半彩色效果，颜色很淡
bg.bw(0, 300);    // 恢复彩色
```

> 和 `saturation(0)` 的区别：视觉效果类似，但 `bw` 是专用的黑白滤镜，可能会更精确。

---

#### `distort(val, time?, intensity?)`

**作用**：让画面产生扭曲变形——像水面波纹、热浪扭曲、梦境眩晕、空间撕裂等效果。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 扭曲的剧烈程度。`0`=画面正常（无扭曲）；`0.3`=轻微波动（像水面的微波）；`0.5`=明显扭曲（像热浪）；`1.0`=强烈扭曲（画面严重变形） |
| `time` | 否 | 从正常到扭曲（或恢复）的渐变耗时（毫秒） |
| `intensity` | 否 | 效果整体混合强度。`1`=完全扭曲；`0.5`=只有一半的扭曲量 |

```
char.distort(0.3, 500);     // 0.5 秒内渐渐出现轻微扭曲（像透过热水蒸气看角色）
char.distort(0.8, 1000);    // 1 秒内强烈扭曲（梦境/眩晕效果）
char.distort(0, 500);       // 0.5 秒恢复正常
```

> 视觉理解：`val` 控制扭曲的**大小/幅度**——数值越大，角色形状歪曲得越厉害。

---

#### `psychedelic(val, time?, intensity?)`

**作用**：让画面产生迷幻的色彩流动效果——色彩像液体一样流动变化。适合幻觉、梦境、嗑药、意识流等场景。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 迷幻程度。`0`=正常画面；越大色彩流动越强烈 |
| `time` | 否 | 过渡耗时（毫秒） |
| `intensity` | 否 | 效果混合强度 |

---

### 4.7 文字属性（仅 Text 类型）

> 以下方法仅对 `Text.set(...)` 创建的文字对象有效。

#### `size(px)` / `px(px)`

**作用**：设置文字大小。

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| `px` | 是 | `32` | 字号。数字越大文字越大，`48` 约等于标题大小 |

```
title.size(48);    // 文字设成 48 号字
title.px(24);      // 另一种写法，效果一样
```

#### `bold(enabled?)`

**作用**：让文字变粗（加粗）。

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| `enabled` | 否 | `true` | 不传参 = 启用粗体；传 `false` = 取消粗体 |

```
title.bold();           // 加粗
title.bold(false);      // 取消加粗
```

#### `italic(enabled?)`

**作用**：让文字倾斜。

#### `uline(enabled?)`

**作用**：在文字下方画一条线（下划线）。

#### `deline(enabled?)`

**作用**：在文字中间画一条横线（删除线）。

---

### 4.8 对话

#### `say(text, audio?)`

**作用**：让这个角色开口说话——对话框里会显示角色的名字和台词。

| 参数 | 必填 | 说明 |
|---|---|---|
| `text` | 是 | 角色要说的话 |
| `audio` | 否 | 说话时播放的语音文件路径 |

```
alice.say("你好，旅行者！", "voice/alice_greeting.mp3");
// 对话框显示"爱丽丝"（displayName）和台词，同时播放语音
```

> 对话框的说话者名字和头像，用 `Character.set()` 时的 `displayName` 和 `avatorPath` 决定。

---

## 5. Audio 对象方法

> 通过 `Audio.set(文件路径)` 创建音频对象。可以控制 BGM（背景音乐）、音效（SE）、语音。

### `play(loop?)`

**作用**：开始播放音频。

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| `loop` | 否 | `0` | `0` = 播完一遍就停；`1` = 循环重复播放 |

```
bgm.play(1);     // BGM 循环播放（一直放）
sfx.play(0);     // 音效只播一次
```

### `stop()`

**作用**：停止播放，声音回到开头（下次 `play()` 从 0 秒开始）。

```
bgm.stop();
```

### `pause()`

**作用**：暂停播放，先停住（下次 `resume()` 从暂停的位置继续）。

```
bgm.pause();
```

### `resume()`

**作用**：从暂停的位置继续播放。

```
bgm.resume();
```

### `fade(duration, targetVolume?)`

**作用**：让音量缓缓变化——可以用来做"声音渐渐变大"（淡入）或"声音渐渐消失"（淡出）。

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| `duration` | 是 | — | 过渡过程耗时（毫秒）。`2000` = 2 秒内慢慢变响/变轻 |
| `targetVolume` | 否 | `1` | 最终要达到的音量：`1`=正常音量（淡入用）；`0`=静音（淡出用）；`0.5`=中间音量 |

```
bgm.fade(2000);         // 2 秒内音量从当前渐渐升到正常（淡入/渐强）
bgm.fade(3000, 0);      // 3 秒内声音渐渐变小直到静音（淡出）
bgm.fade(1000, 0.5);    // 1 秒内渐变到 50% 音量
```

> 实用场景：切换 BGM 时，先 `fade(2000, 0)` 淡出旧音乐，再 `fade(2000)` 淡入新音乐。

---

### `seek(time)`

**作用**：跳转到音频文件的某个时间位置开始播放。

| 参数 | 必填 | 说明 |
|---|---|---|
| `time` | 是 | 跳转的目标时间（秒）。`30` = 从第 30 秒开始播 |

```
bgm.seek(30);     // 跳到音频的第 30 秒
```

---

### `volume(val)`

**作用**：设置当前音量大小。瞬间生效，没有渐变过渡（要用渐变请用 `fade`）。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 音量。范围 `0`（静音）~ `1`（最大音量）。`0.7` = 70% 音量 |

```
bgm.volume(0.7);    // 把音量调到 70%
```

---

### `isPlaying()`

**作用**：查询音频当前是否正在播放（返回是或否）。可以在 `if` 条件里用。

```
let playing = bgm.isPlaying();
if (playing) {
  // 正在播放时要做的事
}
```

---

> **链式写法**：多个方法可以连写在一行，引擎会依次执行：
> ```
> bgm.play().fade(1000).pause();
> ```
> 等效于：
> ```
> bgm.play(1);
> bgm.fade(1000);
> bgm.pause();
> ```

---

## 6. Filter 对象方法

> 通过 `Filter.set()` 创建。与 SceneObject 滤镜的区别：SceneObject 的滤镜只影响**那个对象**（比如只让某个角色变模糊），而 Filter 影响**整个屏幕**（全屏变模糊）。

### `begin()` / `end()`

**作用**：把滤镜应用到画面上 / 移除滤镜效果。

```
fog = Filter.set();
fog.begin();            // 滤镜开始生效

flash = Filter.set();
flash.begin();

// ... 一段时间后
fog.end();              // 移除滤镜
```

### 滤镜方法

Filter 的每个滤镜方法**参数和效果**与 SceneObject 的完全相同：
- `blur(val, time?, intensity?)` — 全屏模糊（视力模糊效果）
- `brightness(val, time?, intensity?)` — 全屏亮度
- `contrast(val, time?, intensity?)` — 全屏对比度
- `saturation(val, time?, intensity?)` — 全屏饱和度
- `gamma(val, time?, intensity?)` — 全屏伽马
- `bw(val, time?, intensity?)` — 全屏黑白
- `distort(val, time?, intensity?)` — 全屏扭曲变形
- `psychedelic(val, time?, intensity?)` — 全屏迷幻色彩
- `rgb(r, g, b, time?, intensity?)` — 全屏颜色叠加
- `hex(color, time?, intensity?)` — 全屏颜色叠加（hex 写法）

详细的参数说明见上文 [4.5 颜色滤镜](#45-颜色滤镜) 和 [4.6 高级滤镜](#46-高级滤镜)，效果完全一样，只是作用范围变成了**整个屏幕**。

### `intensity(val, time?)`

**作用**：调节这个滤镜整体效果的"浓淡程度"。`1` = 完全生效；`0` = 完全无效果。

| 参数 | 必填 | 说明 |
|---|---|---|
| `val` | 是 | 整体强度。`0`=滤镜完全不生效（好像没加一样）；`1`=滤镜全部生效；`0.5`=一半强度 |
| `time` | 否 | 由当前强度渐变到目标强度的耗时（毫秒） |

```
fade.intensity(0.5, 1000);   // 1 秒内滤镜效果渐渐变成一半强度
fade.intensity(1, 500);      // 0.5 秒内恢复完全生效
fade.intensity(0, 1000);     // 1 秒内滤镜渐渐消失
```

---

## 7. ObjectFunction — 自定义对象方法

**作用**：给对象添加你自己的"自定义动作"。比如你想让角色做一个"跳一下"的动作，没有内置函数，你就可以自己定义。

### 语法

```
ObjectFunction [适用的类型::]方法名(参数?) { ... }
```

| 部分 | 说明 |
|---|---|
| `类型列表` | 可选。不写 = 所有对象类型都能用；写 `Character` = 只有角色能用；写 `(Character, Background)` = 角色和背景都能用 |
| `::` | 分隔类型和方法名 |
| `方法名` | 你自己取的名字 |
| `参数` | 可选的参数，加 `?` 后缀表示可以不传（默认为 0） |

### 示例

```
// 定义一个"跳跃"动作，只对角色有效
ObjectFunction Character::jump(height?) {
  this.moveToY(height, 200);   // 向上跳
  this.moveToY(0, 200);        // 落回原位
}

// 定义一个"抖动"动作，角色和背景都能用
ObjectFunction (Background, Character)::shake() {
  this.move(10, 0, 50);     // 往右抖
  this.move(-10, 0, 50);    // 往左抖
  this.move(0, 0, 50);      // 回到原位
}

// 定义一个"淡出"动作，所有对象都能用
ObjectFunction fadeout(time?) {
  this.alpha(0, time);      // 慢慢变透明
}
```

### 调用

定义好之后，像内置方法一样用：

```
alice.jump(100);          // 角色跳 100 像素高
bg.shake();               // 背景抖三下
title.fadeout(500);       // 文字 0.5 秒淡出
```

> 在自定义函数里，`this` 指的是调用该方法的那个对象本身。

---

## 8. 数据类型

### Array（数组/列表）

存一组有序的数据，用 `{ }` 括起来，从 0 开始编号。

```
arr scores = {90, 85, 78};   // 三个分数
scores[0]                     // 访问第 1 个 → 90
scores[1] = 95;              // 把第 2 个改成 95
```

### Map（映射/字典）

存"键→值"的对应关系，可以按名字取值。

```
map config = {
  volume: num = 80;       // 音量 80
  lang: str = "zh";       // 语言中文
};
config.volume              // 取音量值 → 80
config.lang = "en";        // 改成英文
```

### 字面量（直接写的值）

| 类型 | 写法的例子 |
|---|---|
| 数字 | `42`, `3.14`, `-5` |
| 文字 | `"你好"`, `'hello'` |
| 是/否 | `true`（是）, `false`（否） |
| 空值 | `null` |
| 数组 | `{1, 2, 3}` |
| 映射 | `{ key: num = value }` |
| 分组 | `group(a, b, c)` — 用于把多个动作打包给 `parallel`、`sequence` 用 |

### 运算符

| 分类 | 符号 |
|---|---|
| 算术（加减乘除） | `+`, `-`, `*`, `/`, `%`（取余数） |
| 比较（判断大小） | `==`, `!=`, `<`, `<=`, `>`, `>=` |
| 逻辑（且或非） | `&&`（并且）, `\|\|`（或者）, `!`（取反） |
| 赋值（存值） | `=` |

### 标明类型

声明变量时可以用 `: 类型` 说明里面存的是什么数据（可写可不写，写了可以让编辑器帮你检查）：

```
let score: num = 0;          // 数字
let name: str = "Alice";     // 文字
let flag: bool = false;      // 是/否
arr items: num = {1, 2, 3};  // 数字列表
```
